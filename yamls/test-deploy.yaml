apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-updated
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 8
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: redis:latest
        ports:
        - containerPort: 8080
